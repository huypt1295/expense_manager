/// Defines the locale identifiers used by the application.
class LocaleConstants {
  const LocaleConstants._();

  static const en = 'en';
  static const vi = 'vi';
  /// Locale code applied when no explicit preference is provided.
  static const defaultLocale = LocaleConstants.vi;
}
